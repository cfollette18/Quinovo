# Homelab pack — dogfood only

This pack is **not Quinovo**. It is one customer's world: Device, Service, Alert, Tailscale fixture.

CI uses `fixtures/tailscale-status.json`. It never calls live Tailscale.

Action: `acknowledge_alert`.

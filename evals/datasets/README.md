# datasets

Langfuse datasets that fill from Quinovo eval templates. Judges stay under
`templates/`. A dataset here is the test set those judges write when they fail,
and the experiment target they score again on rerun.

| Dataset | Fills when |
|---|---|
| [Agent run](agent_run/) | A catalog judge fails a `quinovo`-tagged agent run |
